import type { CompanionStaticUpgradeScript } from '@companion-module/base'
import type { ScreenFreezeConfig } from './config.js'

export const UpgradeScripts: CompanionStaticUpgradeScript<ScreenFreezeConfig>[] = [
	/*
	 * Place upgrade scripts here.
	 * Remember that once one has been added it cannot be removed!
	 */
]
